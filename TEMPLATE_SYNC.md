# Template sync (Albo OS → template)

Backlog of **"core"** improvements made in Albo OS that are worth pushing back
into the [`albo-ouvre-boite`](https://github.com/Albo-Club/albo-ouvre-boite)
template. This is the **reverse direction** of [`UPGRADING.md`](UPGRADING.md)
(which pulls template changes *into* Albo OS).

There is no automation here on purpose: the agent only **flags** candidates,
Benjamin/Clément **port** the code into the template by hand when they choose
to. Keeping the human in the loop avoids leaking business logic upstream and
lets us generalise the code before it lands in the starter.

## Workflow

1. **At each PR** (see `CLAUDE.md` § "Pre-PR doc audit", question 6): if the
   change touches reusable core code, the agent adds a row to the backlog
   below **and** a short "Template sync" section to the PR description.
2. **When you want to upstream one**: port the code into `albo-ouvre-boite`
   (de-branding / removing any Albo OS specifics as needed), open the PR
   there, then update the row's **Status** here.
3. **Once landed or dropped**: mark the row `✅ upstreamed (#PR)` or
   `❌ dropped (reason)`. Prune rows that are fully resolved when the table
   gets long — git history keeps the trace.

## What qualifies

✅ **Push to the template** — generic, project-agnostic:

- Auth / Better Auth config, multi-tenant plumbing, `convex/lib/` helpers.
- Security (CORS, headers, HMAC webhook verify, rate limiting).
- Reusable UI primitives in `src/components/ui/*`, i18n / SSR patterns.
- DX & tooling: scripts, CI workflows, config, the skills pipeline.
- Bug fixes in code the template also ships.

❌ **Stays in Albo OS** — business/domain specific:

- Portfolio domain: deals, companies, valuations, KPIs, cash, forecasts,
  liabilities, transaction pointage.
- Domain-specific AI tools (`convex/agentTools*.ts`) and their prompts.
- Anything tied to CALTE / Albo Club org structure or seeds.

When a change is *mostly* generic but carries a domain detail, flag the
generic slice only and note what needs stripping before it goes upstream.

## Backlog

| Date       | Candidate | Files (Albo OS) | Why it's generic | Status |
| ---------- | --------- | --------------- | ---------------- | ------ |
| 2026-08-20 | `pnpm.overrides` sur `unstorage` — caret sur préversion + publication amont cassée | `package.json` (`pnpm.overrides`), `KNOWN_ISSUES.md` § « pnpm.overrides » | The template runs the same TanStack Start / nitro stack, so it inherits the exact path: `nitro` depends on `unstorage: ^2.0.0-alpha.7`, and **a caret on a prerelease accepts later prereleases of the same version**, so `alpha.8` walks in on its own. That publish is broken — `dist/index.mjs` imports `destr` five times while the manifest declares **no dependencies at all** — and the build dies at `vite.config.ts` load with `ERR_MODULE_NOT_FOUND`. Falling back to the stable `1.17.5` (which does declare `destr`) is not an option: it is outside nitro's range. Two generalisable lessons worth carrying upstream, both cheap to ship and expensive to rediscover: prerelease ranges need an explicit override rather than trust in semver, and **this bug class is invisible on a warm `node_modules`** — a leftover copy of the missing package satisfies the import, so the local build passes while CI fails (it did here). The durable artefact is the verification rule: any dependency change is checked with `rm -rf node_modules && pnpm install && pnpm build`, never a plain rebuild. **Nothing to strip.** | ⏳ flagged |
| 2026-08-20 | Fenêtre de versions verrouillée `@convex-dev/better-auth` × `better-auth` (correctif de sécurité magic-link) | `package.json` (`"@convex-dev/better-auth": "0.12.2"` exact, `"better-auth": "~1.6.30"`), `KNOWN_ISSUES.md` § « fenêtre de versions verrouillée » | **The template ships both packages and the `magicLink()` plugin**, so it inherits the exact same trap — and the stakes are security, not ergonomics: `better-auth < 1.6.22` carries `GHSA-qq9h-g4jm-xgf3` (**high**, account takeover via pre-account hijacking on magic-link / email-OTP sign-in). The component effectively pins its users to the vulnerable range, because `@convex-dev/better-auth` 0.12.4+ rejects any `better-auth >= 1.6.18` at the `ConvexBetterAuthProvider` `authClient` prop (`useSession().data` collapses to `never`; upstream `get-convex/better-auth#420`). The escape worth carrying upstream is the **exact** adapter pin `0.12.2` — the last version that accepts the patched better-auth — plus two findings a template consumer would otherwise pay for twice: `^`/`~` are **identical on a `0.x` version** (both `>=0.12.2 <0.13.0`) so neither blocks 0.12.3+, and 0.12.3 typechecks fine but slows the `convex-test` harness enough to time out 2-4 of 120 tests, so the compatibility matrix must be run with `test:convex` and not just `tsc`. **Nothing to strip** — the pin and its unblock condition are generic. | ⏳ flagged |
| 2026-08-20 | Package-manager pinning — `packageManager` as the single source of truth | `package.json` (`packageManager`), `.github/workflows/{ci,update-deps,sync-skills}.yml` (drop `version:` from `pnpm/action-setup@v4`), `pnpm-workspace.yaml` (both `allowBuilds` **and** `onlyBuiltDependencies`) | **The template ships the bug** — it is where the `allowBuilds: "set this to true or false"` placeholder came from, and it declares no `packageManager`. Any project derived from it inherits three uncoordinated pnpm versions (local via corepack, CI via a hardcoded `version:`, and on Vercel a version chosen *by project creation date* — a pin that lives outside the repo entirely and can move with no commit). The failure mode is the expensive part: pnpm 11 reads `allowBuilds` and ignores `onlyBuiltDependencies`, a non-boolean value means build refused, and since pnpm 11 runs a `runDepsStatusCheck` before every script, **`ERR_PNPM_IGNORED_BUILDS` exits 1 on every `pnpm <script>` locally** while CI on pnpm 10 stays green. Two non-obvious decisions worth carrying: pin to **whatever Vercel already runs** — Vercel *does* read `packageManager` (CLI ≥ 59, no `ENABLE_EXPERIMENTAL_COREPACK` needed, contrary to what their Corepack page suggests), so pinning to the version it already selected costs zero deploy risk while removing the out-of-repo heuristic; note this also means a later bump of that string **moves production**, and **add no `engines` field at all** — `engines.pnpm` triggers `ERR_PNPM_UNSUPPORTED_ENGINE` when it disagrees with the heuristically-chosen version, and `engines.node` **overrides the Vercel project's Node setting**, so declaring it silently moves the production runtime (here it would have dropped prod from Node 24 to 22). Both arm an outage for no gain; `packageManager` alone does the job. **Nothing to strip** — pure tooling. | ⏳ flagged |
| 2026-08-20 | `if: failure()` issue reporter on scheduled workflows | `.github/workflows/update-deps.yml` (step `Report failure as an issue`, `permissions.issues: write`) | Generalises the `prod-smoke.yml` pattern already flagged (2026-07-30) to the other scheduled job, and the template ships both. A weekly cron that fails in silence is worse than no cron: it *looks* like dependencies are being maintained while nothing lands. Demonstrated here — `update-deps` ran red every Monday for over a month (a `better-auth` bump breaking `pnpm lint`) and nobody saw it, so no dependency update reached the repo in that window. The job was doing its job correctly; only its visibility was missing. Reuses one open issue (comment instead of duplicating) so a long outage doesn't spam. One trap the prod-smoke original doesn't hit and any port must keep: `issues.listForRepo` returns **pull requests too**, and the `chore/update-deps` PR carries this very label — without a `!i.pull_request` filter the alert gets commented onto the PR it is meant to replace. **Nothing to strip.** | ⏳ flagged |
| 2026-08-20 | `.conductor/settings.toml` — workspace setup script | `.conductor/settings.toml` | Every project derived from the template that gets worked on in Conductor (or any git-worktree-per-task setup) starts each workspace with no `node_modules`. Beyond the manual install, that is the window in which an agent that can't find `node_modules` improvises an `npm install` and commits a stray `package-lock.json` next to `pnpm-lock.yaml` — the classic multi-agent lockfile mess. One line of TOML closes it. Two things worth documenting upstream: the file is `.conductor/settings.toml`, **not** `conductor.json` (legacy, ignored by the Mac client once the TOML exists), and gitignored files like `.env.local` must **not** go in the setup script — Conductor's Files-to-copy already carries them via its default `.env*` pattern. **Nothing to strip.** | ⏳ flagged |
| 2026-08-07 | Exposer un texte stocké volumineux sur MCP — fenêtre `offset`/`nextOffset` | `convex/agentTools.ts` (`getDocumentTextInternal`, `DOCUMENT_TEXT_WINDOW`), `convex/mcp/registry.ts` (`getDocumentText`) | Rides on the read-only MCP server the template already ships, plus the semantic-search socle (flagged 2026-07-28) which by construction stores extracted text somewhere. The trap is generic to both: a stored text can reach `MAX_DOCUMENT_CHARS` (900 k chars), and returning it whole makes a ~900 kB JSON-RPC response — over the client's context window and wasteful on the httpAction. The reusable slice is the shape, not the query: a fixed window + `nextOffset` walked until `null`, and the **two distinct truncations kept separate** (`truncated` = the file was cut at extraction, no offset will ever reach the tail; `nextOffset` = there is more to read) — conflating them is exactly how a caller silently reports a partial document as complete. Same pattern for any MCP tool returning a large stored blob (logs, transcripts, raw payloads). **To strip**: the `documents`/`documentTexts` table reads and the org-membership check are Albo-side; upstream keeps the window + the return shape. | ⏳ flagged |
| 2026-08-05 | Embedding request size cap — `MAX_EMBEDDINGS_PER_CALL` via `wrapEmbeddingModel` | `convex/vectorize.ts` | Rides on the semantic-search socle already flagged (2026-07-28), and it is a **latent bug in that socle**, not an Albo specific: `@convex-dev/rag` hands the embedder 100 chunks at a time (hard-coded) and `@openrouter/ai-sdk-provider` leaves `maxEmbeddingsPerCall` unset, so all 100 (~100 k chars) go out in one HTTP request — ~27 k tokens of prose against a 32 k-token endpoint window, i.e. 15 % of margin before the whole ingestion stops. The failure is doubly misleading and worth shipping the fix *before* someone debugs it: pinning one provider (`allow_fallbacks: false`) turns the overflow into a **404 "no endpoints found"** rather than a 400 context error, and 4xx≠408/429 is classified permanent, so it never retries. The fix is one `wrapEmbeddingModel` + a constant, and the non-obvious part is *why it's safe*: the wrapper passes `modelId`/`provider` through, so the RAG namespace identity (namespace, modelId, dimension, filterNames) doesn't move and no backfill is triggered. **Nothing to strip** — no domain coupling. | ⏳ flagged |
| 2026-08-05 | `InlineField` — le `Select` enum doit être contrôlé (correctif : la sélection était perdue) | `src/components/ui/inline-field.tsx` | Bug fix in a primitive the template also ships (the `InlineField` row flagged 2026-07-02). `@radix-ui/react-use-controllable-state` (≥ 1.2) only calls `onValueChange` **synchronously** when the value is controlled; with `defaultValue` it defers the call to a `useEffect`. Any click-to-edit control that closes on selection unmounts the Radix component in that same commit, so the deferred effect never runs and the write is **silently dropped** — no error, no toast, the field just stays empty. Fix is one prop (`value` instead of `defaultValue`). Generic to any inline/popover editor built on Radix, and a trap worth carrying upstream as a comment: **a Radix control whose selection unmounts it must be controlled**. **Nothing to strip** — no domain coupling. | ⏳ flagged |
| 2026-08-04 | MCP tool annotations — the `write` flag → `readOnlyHint` seam | `convex/mcp/registry.ts` (`McpToolAnnotations`, `write` on `defineTool`), `convex/mcp/server.ts` (emit in `tools/list`) | The template ships the read-only MCP server; the moment any project on it exposes a write tool, it hits the same gap: the in-app agent's `needsApproval` has **no equivalent over MCP** (no UI to render the Confirm/Refuse buttons), so a write tool would run with no human checkpoint at all. The seam is ~10 lines and is the spec-compliant answer — `annotations.readOnlyHint: false` (MCP 2025-06-18) is what makes a client ask before running the call. Worth shipping *with* the read-only server so the flag exists before someone needs it, rather than being discovered after the first unconfirmed write. **Nothing to strip** — `defineTool` and the `tools/list` mapping carry no domain coupling; the four Albo write tools themselves stay here (portfolio business logic). | ⏳ flagged |
| 2026-08-03 | `excelToText` / `csvToText` — budget de texte réparti entre onglets | `convex/lib/excel.ts` (+ `tests/excel.test.ts`) | Generic file→text helper with zero domain coupling (in, an `ArrayBuffer` and a filename; out, a string), and it carries a bug class the template ships too: the budget was applied to the **concatenated** output, so the first sheet spent it all and every following sheet vanished silently — a multi-tab workbook read as single-tab. The reusable half is the allocation shape: `fairShares` (max-min fair split, sheets under their share release the rest), headers and cut markers paid up front so every source always appears, and cuts stated in the text rather than silent. The same pattern applies to any output aggregating N sources under one budget (attachments, sections, chunks). **Nothing to strip** — no Convex, no schema, no i18n; the only project tie is importing `MAX_DOCUMENT_CHARS` from `lib/fileText.ts`, which can be a parameter upstream. | ⏳ flagged |
| 2026-07-30 | `LoadingLine` — the spinner-next-to-its-label loading affordance | `src/components/ui/spinner.tsx` | Every app on this stack has the same gap: a Convex query returns `undefined` while it loads, and each call site hand-rolls a muted `<div>` with a "Loading…" string, so the wait reads differently on every screen. `LoadingLine` is 12 lines with no domain coupling — an inline `<span>` so it centres inside a flex parent and inside a `text-center` `TableCell` alike, which is what makes it drop-in for both full-screen guards and empty table rows. Ships next to the `Spinner` already in the file. **Nothing to strip** — the label comes from the caller's `t()`. | ⏳ flagged |
| 2026-07-30 | `attachment` primitive (shadcn base registry, Radix build) + the attachment-card pattern for file lists | `src/components/ui/attachment.tsx`, `src/components/documents/DocumentAttachment.tsx` | Any app that lists uploaded files wants this shape rather than a table (a file is opened, not compared). The primitive is the registry component vendored as-is — worth shipping so the template doesn't need the `base/` registry variant, with the one deviation documented in the file: upstream defaults its action button to the `icon-xs` size, which our `button.tsx` doesn't declare (`icon-sm` instead — the template could add the missing size instead). The card built on top (whole card opens the file via `AttachmentTrigger asChild` over an `<a>`, actions layered above it, kind badge + metadata line) is the reusable half. **Strip before upstream:** `DocumentAttachment` is coupled to our `documents` rows and the OCR reading state — keep the layout, drop the domain props. | ⏳ flagged |
| 2026-07-30 | Dead-code detection (knip) — config + `deadcode` script | `knip.jsonc`, `deadcode` script in `package.json` | Pure DX tooling, project-agnostic. The config encodes the entry-point patterns any project on this stack needs (TanStack Start file-based routes, Convex functions behind the generated `api.*`/`internal.*` proxies, vendored dirs, CSS-only deps) — exactly the non-obvious part worth shipping with the template. Deliberately not wired to CI here (the repo isn't purged yet, so the job would be permanently red); **the template starts clean, so it can ship with a blocking CI job.** **Strip before upstream:** Albo-specific ignores (`convex/migrations/*`). | ⏳ flagged |
| 2026-07-30 | Prod smoke monitoring — daily scheduled run of the e2e smoke against prod | `.github/workflows/prod-smoke.yml` | Any deployed instance of the template wants to know its prod is down before a user does. Daily cron (+ `workflow_dispatch`) replays the template's own `scripts/e2e-smoke.mjs --url` against a `PROD_URL` repo variable; on failure it opens a `prod-smoke`-labelled issue with the check output (comments instead of duplicating if one is open). Zero project coupling — port as-is, just document the `PROD_URL` variable. | ⏳ flagged |
| 2026-07-02 | `InlineField` — click-to-edit field primitive | `src/components/ui/inline-field.tsx` | Generic inline editor (click → format-aware input → Enter/blur save, Esc cancel) usable on any fiche. Ships three resting layouts via `layout`: `stacked` (label above value), `row` (label left / value right + hairline — what a narrow side panel needs to keep long labels on one line) and `block` (no label, full-width paragraph, for a section that already carries its title), plus a `multiline` format that opens a textarea (blur commits, Escape cancels, Enter stays a newline) and a `placeholder` that replaces the em dash on an empty field. All project-agnostic. **Strip before upstream:** decouple from `~/lib/parse` `FieldFormat`/`parseField`/`rawToInput` (instrument display formats) — make the format/parse layer injectable so it isn't tied to the portfolio domain. | ⏳ flagged |
| 2026-07-28 | `useStickyBottom` — side panel that scrolls then freezes at its bottom | `src/hooks/useStickyBottom.ts` | Any app with a sidebar taller than the viewport wants this, and the naive `position: sticky` + `bottom` does **not** work (measured in Chromium: a `bottom` offset never holds a box scrolling upward, and `top`+`bottom` together degrade to `top`). The hook computes the negative `top` offset — `-(panelHeight - portHeight) - gap` — and re-measures via `ResizeObserver` on both the panel and its scrollport, falling back to a positive `top` when the panel fits. It also walks up to the real scroll container instead of assuming the window, which any app-shell layout (`h-svh` + inner `overflow-y-auto`) needs. Zero project coupling — port as-is, with the `KNOWN_ISSUES.md` § « Panneau latéral figé » notes. | ⏳ flagged |
| 2026-07-14 | `normalizeDomain` — messy-domain → bare hostname | `convex/lib/domain.ts` (+ `tests/domain.test.ts`) | Pure, dependency-free: unwraps markdown links `[…](…)`, strips protocol/path/query/`www.`, lowercases, `null` if irreducible. Useful anywhere a domain feeds a logo hotlink or a fetch. No project coupling — port as-is. | ⏳ flagged |
| 2026-07-20 | External-connections core — registry + generic storage + control-tower UI | `convex/lib/connectors.ts`, `convex/connections.ts`, `externalConnections` table (schema), `src/routes/app/$orgSlug/settings/integrations.tsx` (+ `tests/connectors.test.ts`) | Generic infra for any app talking to external platforms: declarative connector registry (scope / auth kind / required keys), one `externalConnections` table with secrets at rest, shared lifecycle (CLI seed + admin-gated in-app connect/disconnect), per-auth-kind `status`/`listIntegrations`, and a registry-driven Integrations settings page (installed vs available, generic connect form). **Strip before upstream:** the Albo-specific registry entries (powens/vasco/notion/docsend) and the `connectionHealth` import from `powens.ts` — ship the core with an empty registry. | ⏳ flagged |
| 2026-07-20 | SSR warm-up cron — keep the Vercel function hot | `convex/warmup.ts`, cron `warm vercel ssr` in `convex/crons.ts` | Any low-traffic internal tool on Vercel pays a 1-3s cold start on most arrivals. A 5-min Convex cron `fetch(SITE_URL)` (with the localhost guard from `auth.ts`) keeps one instance warm. Zero project coupling — port as-is. | ⏳ flagged |
| 2026-07-21 | Webview connector dispatch + OAuth mailbox pattern | `convex/connections.ts` (per-platform webview dispatch in `listIntegrations`/`status`/`syncNow`), `convex/gmail.ts` (OAuth flow: one-shot anti-CSRF state table, HTTP callback route, token refresh with reauth detection — **file removed with the emails feature, recover it from git history before #288**), `gmailOAuthStates` table (schema, still declared inert) | The external-connections core (flagged 2026-07-20) assumed a single webview platform (Powens); the dispatch generalizes it to N webview connectors. The OAuth round-trip (state mutation → provider consent → HTTP callback → upsert + redirect back in-app) is a reusable pattern for any OAuth-connected platform. **Strip before upstream:** the Gmail-specific sync/matching (portfolio business logic stays in Albo OS). | ⏳ flagged |
| 2026-07-21 | Dependency auto-update workflow — weekly validated `pnpm update` PR | `.github/workflows/update-deps.yml`, `scripts/update-deps.mjs` | Same pattern as `sync-skills.yml`: weekly cron runs `pnpm update` (semver ranges only, no major jumps), runs lint/tests/build in the workflow (auto-PRs made with `GITHUB_TOKEN` don't trigger CI), then opens a validated `chore/update-deps` PR listing the bumped packages. Replaces the dormant Renovate app dependency. **Strip before upstream:** the changelog-entry generation (CHANGELOG_PRODUIT.md is Albo OS-specific). | ⏳ flagged |
| 2026-07-20 | Lazy-loaded AI panel — markdown stack off the critical path | `src/routes/app/$orgSlug/route.tsx` (`React.lazy` + `Suspense` around `AiPanel`) | The static `AiPanel` import drags streamdown/ai-elements (~90 kB gz) into the org layout chunk of every app page. The lazy pattern (named-export `.then`) shrinks the layout chunk ~180 → ~12 kB and applies to the template's identical AiPanel wiring. | ⏳ flagged |
| 2026-07-21 | SSR session preload — `initialToken` wiring for TanStack Start | `src/lib/auth-server.ts`, `src/routes/__root.tsx` (getAuth + beforeLoad + provider), `src/router.tsx` (context), `src/routes/api/auth/$.ts` | Official `@convex-dev/better-auth` react-start pattern: server reads the BA cookie on the document request and hands the Convex JWT to `ConvexBetterAuthProvider` via `initialToken`, collapsing the client-side get-session → token → WS-auth waterfall. The `typeof window` guard in beforeLoad (dehydrated context, no per-SPA-nav round trip) is the non-obvious part — port with the KNOWN_ISSUES « Préchargement de session SSR » notes. Zero business coupling. | ⏳ flagged |
| 2026-07-26 | `sync:skills --verify` — offline integrity gate + self-healing sync | `scripts/sync-skills.mjs` (`hashLocal`, `runVerify`, self-heal in `runSync`), `.github/workflows/ci.yml` (job `skills-verify` replaces `skills-drift`), `package.json` | **The template has this exact bug** — its own `sync-skills.mjs` (which we vendored verbatim in #281) compares the lock to *upstream* and never re-hashes what's on disk; `isVendored()` only checks existence. A vendored file edited, truncated or left stale is invisible to both `--check` and the default sync. Demonstrated: appending a line to a `SKILL.md` leaves `--check` green, exit 0. It is what let three Convex `references/` files rot (one 54 lines behind). Fix is ~40 lines: `hashLocal()` mirrors `fetchSkillAt()` against the working tree, `--verify` compares to `computedHash` (no network → can't flake CI), and `runSync` rewrites on local mismatch so a plain `sync:skills` repairs itself. CI split: `--verify` blocks every PR, upstream drift moves to the SessionStart hook + weekly cron. Zero project coupling — port as-is. | ⏳ flagged |
| 2026-07-27 | `--info` color token — blue sibling of `--warning`/`--positive` | `src/styles/brand.css` (`--info` + `--color-info` mapping, light/dark) | Any status system needs a neutral-positive "active/in progress" blue next to the green/red/amber trio; defined once as an oklch token pair, theme-aware. Port as-is. | ⏳ flagged |
| 2026-07-28 | Semantic-search socle — `@convex-dev/rag` + OpenRouter embeddings, org-scoped | `convex/vectorize.ts`, `convex/convex.config.ts` (rag), `convex/agentToolsDocuments.ts` | Generic RAG wiring for any multi-tenant app: one namespace per org, key-based idempotent ingestion (`add` replaces by key), pinned embedding model via the OpenRouter provider already used for chat, membership re-check before every search, delete-on-remove scheduling, per-org backfill. **Strip before upstream:** the `documents`/`companyReports` bindings and the OCR/excel extraction (business pipeline) — ship the RAG instance + search tool + backfill skeleton. | ⏳ flagged |
| 2026-07-30 | Convex regression harness — convex-test + Better Auth component auth | `vitest.config.ts`, `convex/regression.setup.ts`, `.github/workflows/ci.yml` (step `test:convex`), `package.json` | Any Convex + `@convex-dev/better-auth` app wants this exact harness: in-memory `convexTest` with the BA component registered via the official `/test` entry, real-auth identities (`withIdentity({ subject, sessionId })` + component `user`/`session` seed — no mocks, `requireAppUser`/`requireOrgMember` run for real), the multi-dot filename trick to keep tests inside `convex/` without deploying them, and the vitest inline-deps config for `import.meta.glob`. **Strip before upstream:** the Albo OS test files themselves (`regression.*.test.ts` cover domain invariants) — ship the harness, the config and one example tenancy test. | ⏳ flagged |
| 2026-08-03 | Per-user email alert opt-outs — `userPrefs` flags + the members × alerts matrix | `convex/lib/notificationPrefs.ts`, `userPrefs` schema flags, `organizations.listAlertPrefs`/`setMemberAlertPref`, `src/components/settings/AlertPrefsCard.tsx` | Every app that sends recurring email eventually needs "who gets what", and the two non-obvious decisions are worth shipping rather than re-deriving: flags are stored as **opt-OUTs** (absent = subscribed), so adding an alert needs no backfill and a new member is subscribed by default; and they live in `userPrefs`, not on the `users` row, because `requireAppUser` reads that row in every query — a write there re-runs every open subscription (the template already ships that `userPrefs` split for `lastOrgSlug`, this extends it). The `wantsAlert`/`readAlertPrefs` pair is the seam every send site calls, and the settings matrix (rows = members, columns = alerts, own-row-editable / admin-edits-all with the check server-side) is a drop-in screen. **Strip before upstream:** the five Albo alert kinds themselves (cash threshold, overdue entries, bank connection, index failure, report issues) — ship the mechanism with one example kind. Note the trade-off in the header: prefs are global while the editing surface is per-org. | ⏳ flagged |
| 2026-07-29 | `NavLeaf.alsoActiveOn` — multi-route active state for a sidebar entry | `src/components/app-shell/nav.ts` (type + field), `src/components/app-shell/AppSidebar.tsx` (`matchesPath` helper) | Any app whose sidebar entry fronts several routes (a section with sub-tabs) needs the entry highlighted on all of them; the declarative `alsoActiveOn` prefix list + shared exact-or-prefix matcher is app-agnostic. **Strip before upstream:** the Albo-specific nav items — ship the type + matcher only. | ⏳ flagged |
| 2026-08-05 | `usePersistentFilters` — list filters that survive navigation | `src/hooks/usePersistentFilters.ts` | Every app with filterable lists wants this, and the SSR trap is the reason it's worth shipping rather than re-deriving: reading `sessionStorage` during render breaks hydration, so the restore runs in an effect **after** the first render (same pattern as `ThemePicker`). The non-obvious part is the write guard — the state carries the key it was restored for, otherwise the write effect persists the defaults **over** the saved entry on mount (and on every key change). Minimal `[filters, patch, reset]` API + `toggleValue` for multi-select facets; values must be JSON-serializable (arrays, not `Set`s). Zero project coupling — port as-is. | ⏳ flagged |
| 2026-08-07 | Transient-vs-definitive classification for LLM calls + per-step retry budget | `convex/lib/modelRetry.ts` (+ `convex/regression.modelRetry.test.ts`) | Every app that calls a model from a background job hits this: the provider gets saturated or the request is cut, and the naive `try/catch` records a definitive failure — so a three-second hiccup costs a human intervention. Two non-obvious parts are worth shipping rather than re-deriving. (1) Classification reads the error MESSAGE, so it is only trustworthy on errors you did not author — hence `ModelOutputError`, which types the app's own parsing failures so a payload containing the word "timeout" can never classify itself as transient. (2) The retry budget must be carried WITH the step that spent it (`retryStep` next to `retryAttempts`), otherwise every stage of a multi-stage pipeline needs a reset written into it, and a missing reset only shows up on the third incident. **Strip before upstream:** the Albo step names ('identify' / 'analyze') and `reportInbox.retryAfterTransient`, which is coupled to the `inboundEmails` state machine — ship the classifier, the backoff table and the pattern note. | ⏳ flagged |
