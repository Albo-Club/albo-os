node_modules

# env files (never commit; .env.example is tracked)
.env
.env.*
!.env.example

.DS_Store
.cache
.vercel
.output
.nitro
.tanstack

/build/
/api/
/server/build
/public/build
/dist/
/dist-ssr/

# Sentry config file
.env.sentry-build-plugin
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

app.config.timestamp_*.js

# Claude Code local permissions
.claude/settings.local.json
.claude/scheduled_tasks.lock
.env*.local

# Convex prod snapshot exports (live data dumps — NEVER commit, repo is public).
# Produced by the `convex export --prod --path …` safety step in MIGRATIONS.md.
/albo-backup*.zip

# Legal-doc backfill (scripts/backfill-deal-fields.mjs): extraction cache and
# generated reports. Both carry live portfolio data — never commit, repo is
# public. Deleting the cache only costs a re-read of the documents.
/scripts/data/.backfill-cache.json
/backfill-out/

# docs/produit/*.md bundled for the Convex bundler (scripts/gen-product-docs.mjs).
# Regenerated by `pnpm install` and `pnpm dev` — never edit, never commit.
/convex/lib/productDocs.generated.ts
