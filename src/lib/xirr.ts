/**
 * XIRR — re-exported from the Convex-side source of truth (convex/lib/xirr.ts)
 * so the front keeps importing it via `~/lib/xirr` while the implementation
 * lives next to the other pure metric helpers.
 */

export * from '../../convex/lib/xirr'
